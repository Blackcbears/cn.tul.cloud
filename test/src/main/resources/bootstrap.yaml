
server:
  port: 9402
spring:
  profiles:
    active: dev
  application:
    name: tul-oauth2-auth
  cloud:
    nacos:
      discovery:
        server-addr: nacos.gitcui.cn:8848
        group: GoldConfig
      config:
        prefix: Gateway
        server-addr: nacos.gitcui.cn:8848
        file-extension: yaml
        group: GoldConfig
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
management:
  endpoints:
    web:
      exposure:
        include: "*"